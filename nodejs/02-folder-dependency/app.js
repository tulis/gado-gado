/*
 * For folder dependency, if there is no file specified, then "require" will
 * automatically find and use index.js as default file for dependency
 */
var logger = require("./logger");

logger().log("Hello World!!!");
